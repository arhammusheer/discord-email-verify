<div class="container mt-4">
  <div class="row">
    <div class="col-12">
      <h3>
        {{title}}
      </h3>
      <br />
      <p>
        {{message}}
      </p>
      {{#if revertLink}}
      <a href={{revertLink.url}}>{{revertLink.message}}</a>
      {{/if}}
    </div>
  </div>
</div>