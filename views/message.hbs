<div class="container mt-4">
  <div class="row">
    <div class="col-12">
      <h3 class="font-weight-bold text-{{color}}">
        {{title}}
      </h3>
      <br />
      <p style="font-size: large  ;">
        {{message}}
      </p>
      {{#if revertLink}}
      <a href={{revertLink.url}} class="btn btn-primary">{{revertLink.message}}</a>
      {{/if}}
    </div>
  </div>
</div>